{
    Type = FileType;
    Identifier = text;
    BasedOn = file;

    UTI = "public.plain-text";
    Extensions = (
        txt,
        "",
    );
    MIMETypes = (
        "text/plain",
    );
    IsTextFile = YES;
}
